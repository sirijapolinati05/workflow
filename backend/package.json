{
  "name": "workflow-pro-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for WorkFlow Pro",
  "scripts": {
    "dev": ".venv\\Scripts\\uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": ".venv\\Scripts\\uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "install:deps": "pip install -r requirements.txt"
  }
}
